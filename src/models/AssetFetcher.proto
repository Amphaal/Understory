syntax = "proto3";

import "Asset.proto";

service AssetFetcher {
    rpc RequestAssets(AssetsRequest) returns (stream Asset);
}

message AssetsRequest {
    repeated int64 requested_file_hashes = 1;
}